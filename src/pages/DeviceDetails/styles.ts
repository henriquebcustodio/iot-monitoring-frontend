import { createStyles } from '@mantine/core';

const useStyles = createStyles(() => ({
  container: {
    height: '85%',
    width: '100%',
    padding: 0,
    margin: 0,
    maxWidth: 'unset',
    display: 'flex',
  },
}));

export default useStyles;