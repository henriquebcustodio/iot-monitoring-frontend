import { createStyles } from '@mantine/core';

const useStyles = createStyles(() => ({
  container: {
    padding: 0,
    margin: 0,
    display: 'flex',
    width: '100vw',
    height: '100vh',
  }
}));

export default useStyles;